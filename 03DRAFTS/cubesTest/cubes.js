var z = -200;

function Cube(size){
  push();
  // translate(random(-200,200), random(-200,200),random(-200,200))
  translate(0, 0,z)
  fill(255);
  // rotateX(angle);
  // rotateY(angle);
  // rotateY(angle);
  // box(random(100,400) + size);
  box(20 + size);
  pop();



}
